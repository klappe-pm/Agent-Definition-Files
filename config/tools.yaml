# Tools Configuration
# Minimal setup - tools are available but optional

tools:
  # All tools enabled by default for quick start
  enable_all: true
  
# Optional: Fine-grained tool permissions
# Uncomment to restrict specific tools
# permissions:
#   web_search:
#     enabled: true
#     providers: ["google", "bing"]
#   code_execution:
#     enabled: false
#     languages: ["python", "javascript"]
#     sandbox: true
#   file_operations:
#     enabled: true
#     allowed_paths: ["./workspace"]
#     read_only: false
#   api_calls:
#     enabled: true
#     allowed_domains: ["api.github.com", "api.openai.com"]
#     
# Optional: MCP (Model Context Protocol) servers
# mcp_servers:
#   - name: "filesystem"
#     enabled: false
#     config:
#       root_path: "./workspace"
#   - name: "github"
#     enabled: false
#     config:
#       token: "${GITHUB_TOKEN}"
#       
# Optional: Custom tools
# custom_tools:
#   - name: "calculator"
#     enabled: true
#     module: "tools.calculator"
#   - name: "weather"
#     enabled: false
#     module: "tools.weather"
