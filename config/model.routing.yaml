# Model Routing Configuration (ALL OPTIONAL)
# This file defines how tasks are routed to different models
# If not specified, the default model from llm.providers.yaml is used

# Optional: Route specific task types to specific models
# Uncomment and modify as needed
routing:
  # default: claude-3-5-sonnet  # Falls back to provider default if not set
  
  # Task-specific routing (all optional)
  # tasks:
  #   coding:
  #     primary: claude-3-5-sonnet
  #     fallback: [gpt-4o, claude-3-5-haiku]
  #   research:
  #     primary: gpt-4o
  #     fallback: [claude-3-5-haiku]
  #   simple_queries:
  #     primary: gpt-4o-mini
  #   analysis:
  #     primary: claude-3-5-sonnet

# Optional: Budget limits (disabled by default)
# Uncomment to enable cost controls
# budgets:
#   enabled: false  # Set to true to enable budget tracking
#   daily_limit_usd: 100
#   monthly_limit_usd: 3000
#   per_agent_limits:
#     product-manager: 20
#     engineering: 50
#   alerts:
#     warning_threshold: 0.8  # Alert at 80% of budget
#     email: admin@example.com

# Optional: Model selection strategies
# strategies:
#   mode: "cost_optimized"  # Options: "performance", "cost_optimized", "balanced"
#   context_aware: true      # Auto-select based on context length
#   auto_fallback: true      # Automatically fallback on errors
